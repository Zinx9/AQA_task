{
  "name": "sauce-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:api": "npx playwright test --project=API_test --reporter=html --output=reports/api-report",
    "test:ui": "npx playwright test --project=UI_test --reporter=html --output=reports/ui-report"
  },
  "author": "Zino",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/node": "^22.9.0"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
